{
  "mappingType": "sfx-command-mapping.v1",
  "surfaces": {
    "database-invocation": {
      "delivery": "database-memory",
      "operations": ["invoke", "prepare", "circuit", "catalogue"]
    }
  },
  "identities": {
    "capability": { "pattern": "^[a-z][a-z0-9-]*$", "message": "Supply a capability identity." }
  },
  "commands": {
    "capability": {
      "prepare": {
        "min": 1, "max": 1, "identity": "capability", "input": false, "namespace": true,
        "wraps": { "surface": "database-invocation", "operation": "prepare" },
        "description": "Resolve and prove the selected database capability; retain its preparation in SQL. Optional: invocation never consumes it."
      },
      "invoke": {
        "min": 1, "max": 1, "identity": "capability", "input": true, "namespace": true,
        "wraps": { "surface": "database-invocation", "operation": "invoke" },
        "description": "Execute the database capability's declared root through the candidate memory provider; authority is read and planned per invocation."
      },
      "circuit": {
        "min": 1, "max": 1, "identity": "capability", "input": true, "namespace": true,
        "wraps": { "surface": "database-invocation", "operation": "circuit" },
        "description": "Derive the capability's circuits from the selected database authority; every declared scenario in its closure becomes a view."
      },
      "catalogue": {
        "min": 0, "max": 0, "identity": "capability", "input": false, "namespace": false,
        "wraps": { "surface": "database-invocation", "operation": "catalogue" },
        "description": "List the capabilities selected by the current database estate."
      }
    }
  }
}
