# CLI and embodiment parity — 2026-09-08

The work resumed at `CAPSULE_DISCOVERY_FAILED`. Three local repairs were made
in the Harness and CLI: exact external package binding identities, bounded
streaming of bootstrap output without dropping domain outcome fields, and the
declared disposable-parent context required for estate invocation.

| Boundary | Observed result |
| --- | --- |
| Native `sfx capability list` | Exit 0; `LISTED`; 219 capabilities; zero findings |
| CLI transport and entity-neutrality tests | 12 passed |
| Current direct capsule ↔ existing Node embodiment | Four provider-resolution fixtures; four complete outcome matches; 28 assertions passed |
| Repeated embodiment execution | Same full domain outcome on every replay |
| SQL database verification | Verified selected model; 219 managed capabilities |
| Native `sfx capability invoke` | Exit 4; batch materialization requires 4,304 targets, exceeding the admitted maximum of 4,096 |
| Exact finance provisioned token | `PROVIDER_REQUIRED`; four open slots; no bound provider |

Parity exercised `resolve-sidefx-eligible-providers`: undeclared target,
eligible declared target, unsupported target, and unadmitted/unconformed
bindings. It compared current capsule inputs with the database-retained fixture
inputs before execution, then deeply compared complete outcomes from the real
capsule runtime and the existing embodiment. Execution identifiers and kernel
timestamps are separate observation metadata. No fixture provider substitution
was used. The embodiment source was unchanged.

This proves the measured direct runtime agreement, not a completed CLI invocation,
universal embodiment equivalence, live RapidAPI behavior, or managed promotion.
The finance token is absent from the selected managed database catalog; no
scaffold was relabeled as admitted finance capability or embodied as one.

The remaining managed repair is in `operate-capsule-estate` materialization
planning. It builds one estate-wide batch before selecting the requested
capability. The repair must supply a proven necessary closure or bounded batches,
with error and disposable-root release behavior, and repeat the native CLI test.
The materializer's admitted bound was not modified.

[CLI changes and verification](../../sidefx-cli/docs/estate-delivery-verification-2026-09-08.md)
and [Harness finding](../../agentic-harness/docs/cli-estate-invocation-2026-09-08.md)
describe the exact boundaries. Native commands, exit codes, outputs, request
bytes, outcome comparisons, and content digests are retained locally in
[the evidence summary](../evidence/cli-estate-2026-09-08/summary.json).

These are local changes and observations. No managed capability publication or
remote delivery is claimed.
